package com.openmrs.enums;

/**
 * 
 * @author Palpandi
 * 
 * The {@code ModuleType} enum represents different modules or components that may exist within a software application.
 * These predefined module types can be used to categorize and organize various features and functionalities of the application.
 *
 * Usage:
 * <pre>
 * {@code
 * // Assign a module type to a specific feature or functionality in your application
 * ModuleType module = ModuleType.OKR;
 *
 * // Use the module type for categorization and organization within your application or test suite
 * // Example: Identify and execute tests related to the OKR (Objective and Key Results) module.
 * }
 * </pre>
 */
public enum ProjectType {

	OpenMRS
} 

